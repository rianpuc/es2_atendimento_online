# Render Blueprint (Infraestrutura como Código)
# Descreve banco de dados, backend e frontend do agenda-web.
# Docs: https://render.com/docs/blueprint-spec
databases:
  - name: atendimento-db
    databaseName: atendimento_db
    user: atendimento
    plan: free
    region: oregon

services:
  # ===== Backend: Spring Boot (Docker) =====
  - type: web
    name: atendimento-backend
    runtime: docker
    repo: https://github.com/rianpuc/es2_atendimento_online
    branch: deploy
    rootDir: backend
    dockerfilePath: ./Dockerfile
    dockerContext: .
    plan: free
    region: oregon
    healthCheckPath: /api/atendimento
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: atendimento-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: atendimento-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: atendimento-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: atendimento-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: atendimento-db
          property: password

  # ===== Frontend: React (Static Site) =====
  - type: web
    name: atendimento-frontend
    runtime: static
    repo: https://github.com/rianpuc/es2_atendimento_online
    branch: deploy
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: build
    envVars:
      # Injeta o host público do backend; o api.js normaliza para https://host/api
      - key: REACT_APP_API_URL
        fromService:
          type: web
          name: atendimento-backend
          property: host
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
